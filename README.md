# QA-CYPRESS

Creacion de casos de pruebas en el login de la webapp DexManager

## Instalación de dependencias
Instalar las dependecias `npm install`

## Como usar
Usar `npm run cy:open` para iniciar la interfaz de cypress  
Usar `npm run cy:run` para ejecutar los casos headless